'use client'; import {useRouter} from 'next/navigation'; import Header from '@/components/Header'; import {updateSession} from '@/lib/session';
export default function Login(){const r=useRouter();function enter(){updateSession({patient:'Paciente de prueba'});r.push('/session')}return <><Header step="Acceso profesional"/><div className="content space-y-5"><div className="card"><div className="step">1 · Profesional</div><h1 className="text-2xl font-bold mt-2">Entrar a ChainDose</h1><p className="sub">En producción podemos usar WebAuthn/biometría del dispositivo. Por ahora usamos un acceso de demostración.</p></div><button className="btn primary" onClick={enter}>Entrar con biometría</button></div></>}
