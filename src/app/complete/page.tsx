'use client'; import {useEffect,useState} from 'react'; import Link from 'next/link'; import Header from '@/components/Header'; import {clearSession,getSession,Session} from '@/lib/session';
export default function Complete(){const [s,setS]=useState<Session>({});useEffect(()=>setS(getSession()),[]);const diff=s.beforeWeight!=null&&s.afterWeight!=null?(s.beforeWeight-s.afterWeight).toFixed(3):'—';return <><Header step="Sesión completada"/><div className="content space-y-5"><div className="card"><div className="step">6 · Registro cerrado</div><h1 className="text-3xl font-extrabold mt-2">✓ Sesión completa</h1><div className="status text-green-700 bg-green-50 mt-4"><span className="dot"/>Evidencia registrada</div></div><div className="card"><div className="metric"><span>Paciente</span><b>{s.patient||'—'}</b></div><div className="metric"><span>Tag NFC</span><b>{s.tagId||'—'}</b></div><div className="metric"><span>Producto</span><b>{s.product||'—'}</b></div><div className="metric"><span>Peso ANTES</span><b>{s.beforeWeight??'—'} g</b></div><div className="metric"><span>Peso DESPUÉS</span><b>{s.afterWeight??'—'} g</b></div><div className="metric"><span>Diferencia de masa</span><b>{diff} g</b></div></div><button className="btn secondary" onClick={()=>{clearSession();location.href='/session'}}>Nueva sesión</button><Link href="/" className="btn primary block text-center">Inicio</Link></div></>}
