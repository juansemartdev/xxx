'use client';
import {useEffect,useRef,useState} from 'react';
export default function CameraCapture({onCapture}:{onCapture:(dataUrl:string)=>void}){const video=useRef<HTMLVideoElement>(null);const stream=useRef<MediaStream|null>(null);const [error,setError]=useState('');const [active,setActive]=useState(false);
useEffect(()=>()=>stream.current?.getTracks().forEach(t=>t.stop()),[]);
async function start(){try{stream.current=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'},width:{ideal:1280}},audio:false}); if(video.current){video.current.srcObject=stream.current;await video.current.play();setActive(true)}}catch(e){setError('No fue posible acceder a la cámara. Revisa los permisos del navegador.')}}
function capture(){if(!video.current)return;const c=document.createElement('canvas');c.width=video.current.videoWidth;c.height=video.current.videoHeight;c.getContext('2d')?.drawImage(video.current,0,0);onCapture(c.toDataURL('image/jpeg',.9));}
return <div className="space-y-3"><div className="preview relative"><video ref={video} className="w-full h-full object-cover" playsInline muted style={{display:active?'block':'none'}}/>{!active&&<span>Vista previa de cámara</span>}</div>{error&&<p className="text-sm text-red-600">{error}</p>}{!active?<button className="btn secondary" onClick={start}>Activar cámara</button>:<button className="btn primary" onClick={capture}>Tomar fotografía</button>}</div>}
